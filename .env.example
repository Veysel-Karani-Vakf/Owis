# Supabase (public keys — safe for the browser bundle)
VITE_SUPABASE_URL=https://goyqlyrtclpywvolayzu.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdveXFseXJ0Y2xweXd2b2xheXp1Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODcyMTY3MTQsImV4cCI6MjEwMjc5MjcxNH0.1XFsXrUFbfcMAVKrjO7M-WTDnGRN4QSHGDoiTJqSoIs

# Storage bucket used by the admin dashboard for uploaded media
VITE_SUPABASE_MEDIA_BUCKET=media

# --- Local admin bootstrap (server-side only; never expose in the browser). ---
# Get the service role key from Supabase > Project Settings > API.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# ADMIN_EMAIL=vktysv@gmail.com
# ADMIN_PASSWORD=choose-a-secure-password

# --- Server-side only (never bundled). Used by scripts/ for schema + seeding. ---
# Paste the full Postgres connection string from Supabase > Settings > Database > URI
# SUPABASE_DB_URL=postgresql://postgres.goyqlyrtclpywvolayzu:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres

# --- Payment gateway (api/ functions; server-side only, set them in Vercel). ---
# mock = internal simulated bank (default), test/production = real İş Bank POS.
# PAYMENT_MODE=mock
# PAYMENT_CLIENT_ID=merchant-id-from-the-bank
# PAYMENT_STORE_KEY=store-key-from-the-bank
# PAYMENT_GATE_URL=https://sanalpos.isbank.com.tr/fim/est3Dgate
# The payment functions also need SUPABASE_SERVICE_ROLE_KEY (see above).
